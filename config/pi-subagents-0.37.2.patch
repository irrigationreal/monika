diff --git a/src/extension/fanout-child.ts b/src/extension/fanout-child.ts
index 0da9904..83bab49 100644
--- a/src/extension/fanout-child.ts
+++ b/src/extension/fanout-child.ts
@@ -15,6 +15,13 @@ import { loadConfig } from "./config.ts";
 import { type Details, type SubagentState } from "../shared/types.ts";
 
 function getSubagentSessionRoot(parentSessionFile: string | null): string {
+	const configuredRoot = process.env.PI_SUBAGENT_SESSION_ROOT?.trim();
+	if (configuredRoot) {
+		const expandedRoot = configuredRoot.startsWith("~/")
+			? path.join(os.homedir(), configuredRoot.slice(2))
+			: configuredRoot;
+		return path.resolve(expandedRoot);
+	}
 	if (parentSessionFile) {
 		const baseName = path.basename(parentSessionFile, ".jsonl");
 		const sessionsDir = path.dirname(parentSessionFile);
diff --git a/src/extension/index.ts b/src/extension/index.ts
index eff8c2d..d8c9d99 100644
--- a/src/extension/index.ts
+++ b/src/extension/index.ts
@@ -84,6 +84,13 @@ export { loadConfig } from "./config.ts";
  * Falls back to a unique temp directory if no parent session.
  */
 function getSubagentSessionRoot(parentSessionFile: string | null): string {
+	const configuredRoot = process.env.PI_SUBAGENT_SESSION_ROOT?.trim();
+	if (configuredRoot) {
+		const expandedRoot = configuredRoot.startsWith("~/")
+			? path.join(os.homedir(), configuredRoot.slice(2))
+			: configuredRoot;
+		return path.resolve(expandedRoot);
+	}
 	if (parentSessionFile) {
 		const baseName = path.basename(parentSessionFile, ".jsonl");
 		const sessionsDir = path.dirname(parentSessionFile);
@@ -320,7 +327,10 @@ export default function registerSubagentExtension(pi: ExtensionAPI): void {
 
 	pi.registerMessageRenderer<SubagentNotifyDetails>("subagent-notify", (message, options, theme) => {
 		const content = typeof message.content === "string" ? message.content : "";
-		const details = (message.details as SubagentNotifyDetails | undefined) ?? parseSubagentNotifyContent(content);
+		const rawDetails = message.details as (SubagentNotifyDetails & { runIds?: string[] }) | undefined;
+		const details = rawDetails && typeof rawDetails.resultPreview === "string"
+			? rawDetails
+			: parseSubagentNotifyContent(content);
 		if (!details) return new Text(content, 0, 0);
 		const icon = details.status === "completed"
 			? theme.fg("success", "✓")
@@ -459,7 +469,7 @@ export default function registerSubagentExtension(pi: ExtensionAPI): void {
 	registerWaitTool(pi, state, waitToolConfig.enabled);
 
 	pi.on("agent_end", async (_event, ctx) => {
-		if (ctx.hasUI) return;
+		if (ctx.hasUI || process.env.PI_SUBAGENTS_DISABLE_AUTO_DRAIN === "1") return;
 		await drainOutstandingWork({ state, events: pi.events });
 	});
 
@@ -562,7 +572,9 @@ export default function registerSubagentExtension(pi: ExtensionAPI): void {
 		scheduledRunManager.bindSession(ctx);
 		restoreSlashFinalSnapshots(ctx.sessionManager.getEntries());
 		startResultWatcher();
-		primeExistingResults({ triggerTurn: !recovering });
+		primeExistingResults({
+			triggerTurn: !recovering || process.env.PI_SUBAGENTS_TRIGGER_RECOVERED_RESULTS === "1",
+		});
 		fleetStatus?.setContext(ctx);
 	};
 
diff --git a/src/runs/background/async-execution.ts b/src/runs/background/async-execution.ts
index 34213ec..007fb8c 100644
--- a/src/runs/background/async-execution.ts
+++ b/src/runs/background/async-execution.ts
@@ -405,6 +405,41 @@ function terminateRunnerBeforeProceed(pid: number): void {
 	}
 }
 
+function readRuntimeInstance(): { id: string; createdAt: number } | undefined {
+	const file = process.env.MONIKA_RUNTIME_INSTANCE_FILE?.trim() || "/run/monika-runtime-instance.json";
+	try {
+		const value = JSON.parse(fs.readFileSync(file, "utf-8")) as { id?: unknown; createdAt?: unknown };
+		if (typeof value.id === "string" && typeof value.createdAt === "number") return { id: value.id, createdAt: value.createdAt };
+	} catch { /* Non-container callers have no runtime epoch. */ }
+	return undefined;
+}
+
+function persistLaunchIntent(input: { id: string; asyncDir: string; sessionId?: string; mode: string }): string {
+	const runnerProcessInstanceId = randomUUID();
+	const runtime = readRuntimeInstance();
+	writePrivateAtomicJson(path.join(input.asyncDir, "launch.json"), {
+		lifecycleArtifactVersion: 1, runId: input.id, sessionId: input.sessionId,
+		asyncDir: input.asyncDir, mode: input.mode, state: "registered", runnerProcessInstanceId,
+		runtimeInstanceId: runtime?.id, runtimeCreatedAt: runtime?.createdAt, registeredAt: Date.now(),
+	});
+	return runnerProcessInstanceId;
+}
+
+function markRunnerNotStarted(asyncDir: string, runId: string, runnerProcessInstanceId: string): void {
+	writePrivateAtomicJson(path.join(asyncDir, "process-terminal.json"), {
+		version: 1, state: "not-started", runId, runnerProcessInstanceId,
+	});
+}
+
+function linuxProcessStartTicks(pid: number | undefined): string | undefined {
+	if (!pid || process.platform !== "linux") return undefined;
+	try {
+		const stat = fs.readFileSync(`/proc/${pid}/stat`, "utf-8");
+		const close = stat.lastIndexOf(")");
+		return stat.slice(close + 2).split(/\s+/)[19];
+	} catch { return undefined; }
+}
+
 function spawnRunner(cfg: object, suffix: string, cwd: string, onProcessTerminal?: (proof: unknown) => void): { pid?: number; error?: string } {
 	if (!jitiCliPath) {
 		return { error: "upstream jiti for TypeScript execution could not be found; ensure package dependencies are installed" };
@@ -421,8 +456,19 @@ function spawnRunner(cfg: object, suffix: string, cwd: string, onProcessTerminal
 
 	fs.mkdirSync(TEMP_ROOT_DIR, { recursive: true });
 	const cfgPath = getAsyncConfigPath(suffix);
-	const runnerProcessInstanceId = randomUUID();
+	const configuredInstanceId = (cfg as { runnerProcessInstanceId?: unknown }).runnerProcessInstanceId;
+	const runnerProcessInstanceId = typeof configuredInstanceId === "string" ? configuredInstanceId : randomUUID();
 	const launchConfig = { ...cfg, runnerProcessInstanceId };
+	const launchIdentity = launchConfig as { id?: unknown; asyncDir?: unknown; sessionId?: unknown; resultMode?: unknown };
+	const runtime = readRuntimeInstance();
+	if (typeof launchIdentity.id === "string" && typeof launchIdentity.asyncDir === "string") {
+		writePrivateAtomicJson(path.join(launchIdentity.asyncDir, "launch.json"), {
+			lifecycleArtifactVersion: 1, runId: launchIdentity.id, sessionId: launchIdentity.sessionId,
+			asyncDir: launchIdentity.asyncDir, mode: launchIdentity.resultMode, state: "registered",
+			runnerProcessInstanceId, runtimeInstanceId: runtime?.id, runtimeCreatedAt: runtime?.createdAt,
+			registeredAt: Date.now(),
+		});
+	}
 	fs.writeFileSync(cfgPath, JSON.stringify(launchConfig));
 	const runner = path.join(path.dirname(fileURLToPath(import.meta.url)), "subagent-runner.ts");
 	const nodeCommand = resolveAsyncRunnerNodeCommand();
@@ -457,6 +503,20 @@ function spawnRunner(cfg: object, suffix: string, cwd: string, onProcessTerminal
 		});
 		closeFd(stdoutFd);
 		closeFd(stderrFd);
+		if (typeof launchIdentity.id === "string" && typeof launchIdentity.asyncDir === "string") {
+			try {
+				writePrivateAtomicJson(path.join(launchIdentity.asyncDir, "launch.json"), {
+					lifecycleArtifactVersion: 1, runId: launchIdentity.id, sessionId: launchIdentity.sessionId,
+					asyncDir: launchIdentity.asyncDir, mode: launchIdentity.resultMode, state: "spawned",
+					runnerProcessInstanceId, runtimeInstanceId: runtime?.id, runtimeCreatedAt: runtime?.createdAt,
+					pid: proc.pid, processStartTicks: linuxProcessStartTicks(proc.pid), registeredAt: Date.now(), updatedAt: Date.now(),
+				});
+			} catch (error) {
+				// The registered intent remains a fail-closed blocker and status.json
+				// will shortly supply the PID. Never orphan a successfully spawned runner.
+				console.error(`[pi-subagents] failed to update durable launch identity: ${error instanceof Error ? error.message : String(error)}`);
+			}
+		}
 		proc.on("error", (error) => {
 			console.error(`[pi-subagents] async spawn failed: ${error.message}`);
 		});
@@ -873,6 +933,7 @@ export function executeAsyncChain(
 	id: string,
 	params: AsyncChainParams,
 ): AsyncExecutionResult {
+	if (process.env.PI_SUBAGENTS_HOST_DRAINING === "1") return formatAsyncStartError("chain", "Agent host is draining; async launches are temporarily disabled.");
 	const {
 		chain,
 		agents,
@@ -964,11 +1025,19 @@ export function executeAsyncChain(
 		return [childIntercomTarget(step.agent, childTargetIndex++)];
 	}) : undefined;
 
+	let runnerProcessInstanceId: string;
+	try {
+		runnerProcessInstanceId = persistLaunchIntent({ id, asyncDir, sessionId: ctx.currentSessionId, mode: resultMode });
+		ctx.pi.events.emit("subagent:async-registering", { id, sessionId: ctx.currentSessionId, mode: resultMode, asyncDir });
+	} catch (error) {
+		return formatAsyncStartError(resultMode, `Failed to persist async launch intent '${id}': ${error instanceof Error ? error.message : String(error)}`);
+	}
 	let spawnResult: { pid?: number; error?: string } = {};
 	try {
 		spawnResult = spawnRunner(
 			{
 				id,
+				runnerProcessInstanceId,
 				steps,
 				resultPath: inheritedNestedRoute ? nestedResultsPath(inheritedNestedRoute.rootRunId, id) : path.join(RESULTS_DIR, `${id}.json`),
 				cwd: runnerCwd,
@@ -1011,10 +1080,12 @@ export function executeAsyncChain(
 		);
 	} catch (error) {
 		const message = error instanceof Error ? error.message : String(error);
+		try { markRunnerNotStarted(asyncDir, id, runnerProcessInstanceId); } catch {}
 		return formatAsyncStartError(resultMode, `Failed to start async ${resultMode} '${id}': ${message}`);
 	}
 
 	if (spawnResult.error) {
+		try { markRunnerNotStarted(asyncDir, id, runnerProcessInstanceId); } catch {}
 		return formatAsyncStartError(resultMode, `Failed to start async ${resultMode} '${id}': ${spawnResult.error}`);
 	}
 
@@ -1130,6 +1201,7 @@ export function executeAsyncSingle(
 	id: string,
 	params: AsyncSingleParams,
 ): AsyncExecutionResult {
+	if (process.env.PI_SUBAGENTS_HOST_DRAINING === "1") return formatAsyncStartError("single", "Agent host is draining; async launches are temporarily disabled.");
 	const {
 		agent,
 		agentConfig,
@@ -1304,11 +1376,19 @@ export function executeAsyncSingle(
 	} catch (error) {
 		return formatAsyncStartError("single", `Failed to persist async recovery descriptor for '${id}': ${error instanceof Error ? error.message : String(error)}`);
 	}
+	let runnerProcessInstanceId: string;
+	try {
+		runnerProcessInstanceId = persistLaunchIntent({ id, asyncDir, sessionId: ctx.currentSessionId, mode: "single" });
+		ctx.pi.events.emit("subagent:async-registering", { id, sessionId: ctx.currentSessionId, mode: "single", asyncDir });
+	} catch (error) {
+		return formatAsyncStartError("single", `Failed to persist async launch intent '${id}': ${error instanceof Error ? error.message : String(error)}`);
+	}
 	let spawnResult: { pid?: number; error?: string } = {};
 	try {
 		spawnResult = spawnRunner(
 			{
 				id,
+				runnerProcessInstanceId,
 				steps: [
 					{
 						parentSessionId: ctx.parentSessionId ?? ctx.currentSessionId,
@@ -1385,10 +1465,12 @@ export function executeAsyncSingle(
 		);
 	} catch (error) {
 		const message = error instanceof Error ? error.message : String(error);
+		try { markRunnerNotStarted(asyncDir, id, runnerProcessInstanceId); } catch {}
 		return formatAsyncStartError("single", `Failed to start async run '${id}': ${message}`);
 	}
 
 	if (spawnResult.error) {
+		try { markRunnerNotStarted(asyncDir, id, runnerProcessInstanceId); } catch {}
 		return formatAsyncStartError("single", `Failed to start async run '${id}': ${spawnResult.error}`);
 	}
 
diff --git a/src/runs/background/notify.ts b/src/runs/background/notify.ts
index 1582ccd..eac578c 100644
--- a/src/runs/background/notify.ts
+++ b/src/runs/background/notify.ts
@@ -145,6 +145,7 @@ export function formatGroupedCompletion(details: SubagentNotifyDetails[]): strin
 
 interface PendingCompletion {
 	key: string;
+	runId: string;
 	details: SubagentNotifyDetails;
 	triggerTurn: boolean;
 	resolve(accepted: boolean): void;
@@ -160,8 +161,9 @@ function sendCompletion(pi: Pick<ExtensionAPI, "sendMessage">, items: PendingCom
 				customType: "subagent-notify",
 				content,
 				display: true,
+				details: { version: 1, runIds: items.map((item) => item.runId) },
 			},
-			{ triggerTurn: items.some((item) => item.triggerTurn) },
+			{ triggerTurn: items.some((item) => item.triggerTurn), deliverAs: "followUp" },
 		);
 		return true;
 	} catch {
@@ -265,6 +267,7 @@ export default function registerSubagentNotify(
 		pending.set(key, completion);
 		const item: PendingCompletion = {
 			key,
+			runId: String(result.id ?? "unknown"),
 			details,
 			triggerTurn: result.triggerTurn !== false,
 			resolve,
diff --git a/src/runs/background/result-watcher.ts b/src/runs/background/result-watcher.ts
index 86e8350..4244328 100644
--- a/src/runs/background/result-watcher.ts
+++ b/src/runs/background/result-watcher.ts
@@ -160,6 +160,7 @@ export function createResultWatcher(
 				state.completionSeen.delete(completionKey);
 			} else if (lastSeenAt !== undefined) {
 				if (!ownsSession(data.sessionId, epoch) || !fsApi.existsSync(resultPath)) return;
+				if (process.env.PI_SUBAGENTS_HOST_ACK_RESULTS === "1") return;
 				try {
 					fsApi.unlinkSync(resultPath);
 				} catch (error) {
@@ -203,6 +204,14 @@ export function createResultWatcher(
 				};
 			}), nestedChildren);
 
+			// Agentd installs this barrier before evaluating drain quiescence. Keep
+			// the durable result untouched and retry after drain cancellation rather
+			// than starting an internal completion continuation during backup/deploy.
+			if (process.env.PI_SUBAGENTS_HOST_DRAINING === "1") {
+				scheduleResult(file, triggerTurn, RETRY_DELAY_MS);
+				return;
+			}
+
 			const intercomTarget = data.intercomTarget?.trim();
 			let intercomDelivered = false;
 			if (deliverIntercomResults && intercomTarget && triggerTurn) {
@@ -273,6 +282,7 @@ export function createResultWatcher(
 				console.error(`Completion observer failed for '${resultPath}':`, error);
 			}
 			if (!ownsSession(data.sessionId, epoch) || !fsApi.existsSync(resultPath)) return;
+			if (process.env.PI_SUBAGENTS_HOST_ACK_RESULTS === "1") return;
 			try {
 				fsApi.unlinkSync(resultPath);
 			} catch (error) {
diff --git a/src/runs/background/subagent-runner.ts b/src/runs/background/subagent-runner.ts
index 6efb0bd..c04850b 100644
--- a/src/runs/background/subagent-runner.ts
+++ b/src/runs/background/subagent-runner.ts
@@ -1449,37 +1449,33 @@ async function runSingleStep(
 					? (finalResult?.error ? `${finalResult.error}\n${acceptanceFailure}` : acceptanceFailure)
 					: finalResult?.error;
 
+	let artifactWriteError: string | undefined;
 	if (artifactPaths && ctx.artifactConfig?.enabled !== false) {
-		if (ctx.artifactConfig?.includeOutput !== false) {
-			fs.writeFileSync(artifactPaths.outputPath, formatOutputArtifactContent({
-				output,
-				error: effectiveFinalError,
-				transcriptPath: transcriptWriter ? artifactPaths.transcriptPath : undefined,
-				metadataPath: ctx.artifactConfig?.includeMetadata === false ? undefined : artifactPaths.metadataPath,
-			}), "utf-8");
-		}
-		if (ctx.artifactConfig?.includeMetadata !== false) {
-			fs.writeFileSync(
-				artifactPaths.metadataPath,
-				JSON.stringify({
-					runId: ctx.id,
-					agent: step.agent,
-					task,
-					exitCode: effectiveFinalExitCode,
-					model: finalResult?.model,
-					attemptedModels: attemptedModels.length > 0 ? attemptedModels : undefined,
-					modelAttempts,
-					error: effectiveFinalError,
-					acceptance: effectiveAcceptance,
+		try {
+			// Children can remove project-local artifact directories. Optional
+			// diagnostics must never abort mandatory lifecycle finalization.
+			fs.mkdirSync(path.dirname(artifactPaths.outputPath), { recursive: true });
+			if (ctx.artifactConfig?.includeOutput !== false) {
+				fs.writeFileSync(artifactPaths.outputPath, formatOutputArtifactContent({
+					output, error: effectiveFinalError,
+					transcriptPath: transcriptWriter ? artifactPaths.transcriptPath : undefined,
+					metadataPath: ctx.artifactConfig?.includeMetadata === false ? undefined : artifactPaths.metadataPath,
+				}), "utf-8");
+			}
+			if (ctx.artifactConfig?.includeMetadata !== false) {
+				fs.writeFileSync(artifactPaths.metadataPath, JSON.stringify({
+					runId: ctx.id, agent: step.agent, task, exitCode: effectiveFinalExitCode,
+					model: finalResult?.model, attemptedModels: attemptedModels.length > 0 ? attemptedModels : undefined,
+					modelAttempts, error: effectiveFinalError, acceptance: effectiveAcceptance,
 					...(capabilityAudit ? { capabilityCeiling: capabilityAudit.ceiling, capabilityAudit } : {}),
 					launchContractDigest: actualLaunchContractDigest,
 					...(transcriptWriter ? { transcriptPath: artifactPaths.transcriptPath } : {}),
-					transcriptError: transcriptWriter?.getError(),
-					skills: step.skills,
-					timestamp: Date.now(),
-				}, null, 2),
-				"utf-8",
-			);
+					transcriptError: transcriptWriter?.getError(), skills: step.skills, timestamp: Date.now(),
+				}, null, 2), "utf-8");
+			}
+		} catch (error) {
+			artifactWriteError = error instanceof Error ? error.message : String(error);
+			console.error(`Optional subagent artifact write failed for '${ctx.id}': ${artifactWriteError}`);
 		}
 	}
 
@@ -1500,7 +1496,7 @@ async function runSingleStep(
 		totalCost: costSummaryFromAttempts(modelAttempts),
 		artifactPaths,
 		transcriptPath: transcriptWriter ? artifactPaths?.transcriptPath : undefined,
-		transcriptError: transcriptWriter?.getError(),
+		transcriptError: [transcriptWriter?.getError(), artifactWriteError].filter(Boolean).join("; ") || undefined,
 		interrupted: timedOutAfterAcceptance || stoppedAfterAcceptance || turnBudgetExceeded ? false : finalResult?.interrupted,
 		timedOut: timedOutAfterAcceptance ? true : finalResult?.timedOut,
 		stopped: stoppedAfterAcceptance ? true : finalResult?.stopped,
diff --git a/src/runs/foreground/subagent-executor.ts b/src/runs/foreground/subagent-executor.ts
index b2bb70d..aeaecf8 100644
--- a/src/runs/foreground/subagent-executor.ts
+++ b/src/runs/foreground/subagent-executor.ts
@@ -3940,6 +3940,7 @@ export function createSubagentExecutor(deps: ExecutorDeps): {
 		if (spawnPreflight.error) return spawnBudgetErrorResult(spawnPreflight.error, foregroundMode);
 
 		let forkSessionFileForIndex: (idx?: number) => string | undefined = () => undefined;
+		let forkSessionDirForIndex: ((idx?: number) => string) | undefined;
 		let forkThinkingOverrideForIndex: (idx?: number) => AgentConfig["thinking"] | undefined = () => undefined;
 		let prepareForkThinking = (_agentName: string, _index: number, _modelOverride?: string): void => {};
 		const forkThinkingRequirements = new Map<number, boolean>();
@@ -3970,6 +3971,7 @@ export function createSubagentExecutor(deps: ExecutorDeps): {
 			};
 			const forkContextResolver = createForkContextResolver(ctx.sessionManager, contextPolicy.usesFork ? "fork" : undefined, {
 				forceThinkingOffForIndex: (index) => forkThinkingRequirements.get(index) ?? true,
+				sessionDirForIndex: (index) => forkSessionDirForIndex?.(index),
 			});
 			forkSessionFileForIndex = forkContextResolver.sessionFileForIndex;
 			forkThinkingOverrideForIndex = forkContextResolver.thinkingOverrideForIndex;
@@ -4014,6 +4016,7 @@ export function createSubagentExecutor(deps: ExecutorDeps): {
 		}
 		const sessionDirForIndex = (idx?: number) =>
 			path.join(sessionRoot, `run-${idx ?? 0}`);
+		forkSessionDirForIndex = sessionDirForIndex;
 		const forkSessionFileForTask: ForkSessionFileForTask = (agentName, idx = 0, modelOverride) => {
 			if (!shouldForkAgent(contextPolicy, agentName)) return undefined;
 			prepareForkThinking(agentName, idx, modelOverride);
diff --git a/src/shared/fork-context.ts b/src/shared/fork-context.ts
index b43f170..b3bfe58 100644
--- a/src/shared/fork-context.ts
+++ b/src/shared/fork-context.ts
@@ -36,6 +36,8 @@ interface ForkableSessionManager {
 
 interface ForkContextResolverOptions {
 	openSession?: (path: string, sessionDir?: string) => BranchSessionManager;
+	/** Resolve the isolated destination directory for each forked child. */
+	sessionDirForIndex?: (index: number) => string | undefined;
 	/** Decide per child index whether a sanitized transcript must also disable the child's
 	 * thinking. Defaults to true (the pre-existing conservative behavior) when omitted. */
 	forceThinkingOffForIndex?: (index: number) => boolean;
@@ -152,7 +154,7 @@ export function createForkContextResolver(
 	const openSession = options.openSession
 		?? sessionManager.openSession
 		?? ((file: string, dir?: string) => SessionManager.open(file, dir));
-	const sessionDir = sessionManager.getSessionDir?.();
+	const fallbackSessionDir = sessionManager.getSessionDir?.();
 	const cachedResolutions = new Map<number, ForkContextResolution>();
 
 	const resolveFork = (index = 0): ForkContextResolution => {
@@ -162,6 +164,7 @@ export function createForkContextResolver(
 			if (!fs.existsSync(parentSessionFile)) {
 				throw new Error(`Parent session file does not exist: ${parentSessionFile}. Pi has not persisted enough history to fork yet.`);
 			}
+			const sessionDir = options.sessionDirForIndex?.(index) ?? fallbackSessionDir;
 			const sourceManager = openSession(parentSessionFile, sessionDir);
 			const sessionFile = sourceManager.createBranchedSession(leafId);
 			if (!sessionFile) {
diff --git a/src/shared/types.ts b/src/shared/types.ts
index 30a3087..2fa619b 100644
--- a/src/shared/types.ts
+++ b/src/shared/types.ts
@@ -1578,7 +1578,9 @@ export function resolveTempScopeId(options?: {
 
 const MAX_PARALLEL = 8;
 export const MAX_CONCURRENCY = 4;
-export const TEMP_ROOT_DIR = path.join(os.tmpdir(), `pi-subagents-${resolveTempScopeId()}`);
+export const TEMP_ROOT_DIR = process.env.PI_SUBAGENT_RUNTIME_ROOT?.trim()
+	? path.resolve(process.env.PI_SUBAGENT_RUNTIME_ROOT.trim())
+	: path.join(os.tmpdir(), `pi-subagents-${resolveTempScopeId()}`);
 export const RESULTS_DIR = path.join(TEMP_ROOT_DIR, "async-subagent-results");
 export const ASYNC_DIR = path.join(TEMP_ROOT_DIR, "async-subagent-runs");
 export const CHAIN_RUNS_DIR = path.join(TEMP_ROOT_DIR, "chain-runs");
diff --git a/test/integration/fork-context-execution.test.ts b/test/integration/fork-context-execution.test.ts
index ebd0f07..7d04310 100644
--- a/test/integration/fork-context-execution.test.ts
+++ b/test/integration/fork-context-execution.test.ts
@@ -206,8 +206,9 @@ describe("fork context execution wiring", { skip: !available ? "subagent executo
 		return sessionFile;
 	}
 
-	function makeForkingSessionManagerRecorder(options: { sessionFile: string; leafId: string }) {
+	function makeForkingSessionManagerRecorder(options: { sessionFile: string; leafId: string; useSessionDir?: boolean }) {
 		const openedPaths: string[] = [];
+		const openedSessionDirs: Array<string | undefined> = [];
 		const branchedLeafIds: string[] = [];
 		let counter = 0;
 		fs.mkdirSync(path.dirname(options.sessionFile), { recursive: true });
@@ -216,20 +217,22 @@ describe("fork context execution wiring", { skip: !available ? "subagent executo
 			getSessionId: () => "session-123",
 			getSessionFile: () => options.sessionFile,
 			getLeafId: () => options.leafId,
-			openSession: (sessionFile: string) => {
+			openSession: (sessionFile: string, sessionDir?: string) => {
 				openedPaths.push(sessionFile);
+				openedSessionDirs.push(sessionDir);
 				return {
 					createBranchedSession: (leafId: string) => {
 						branchedLeafIds.push(leafId);
 						counter++;
-						const childSessionFile = path.join(tempDir, `fork-${counter}.jsonl`);
+						const childSessionFile = path.join(options.useSessionDir ? (sessionDir ?? tempDir) : tempDir, `fork-${counter}.jsonl`);
+						fs.mkdirSync(path.dirname(childSessionFile), { recursive: true });
 						fs.writeFileSync(childSessionFile, '{"type":"session","version":1,"id":"child","timestamp":"2026-04-16T00:00:00.000Z","cwd":"/tmp"}\n', "utf-8");
 						return childSessionFile;
 					},
 				};
 			},
 		};
-		return { manager, openedPaths, branchedLeafIds };
+		return { manager, openedPaths, openedSessionDirs, branchedLeafIds };
 	}
 
 	function writeAgent(projectRoot: string, name: string, model: string): void {
@@ -1152,9 +1155,10 @@ describe("fork context execution wiring", { skip: !available ? "subagent executo
 	});
 
 	it("creates one forked session for single mode", async () => {
-		const { manager, openedPaths, branchedLeafIds } = makeForkingSessionManagerRecorder({
+		const { manager, openedPaths, openedSessionDirs, branchedLeafIds } = makeForkingSessionManagerRecorder({
 			sessionFile: path.join(tempDir, "parent.jsonl"),
 			leafId: "leaf-123",
+			useSessionDir: true,
 		});
 		const executor = makeExecutor();
 
@@ -1175,12 +1179,15 @@ describe("fork context execution wiring", { skip: !available ? "subagent executo
 		assert.notEqual(args[sessionIndex + 1], path.join(tempDir, "parent.jsonl"));
 		assert.ok(args[sessionIndex + 1]);
 		assert.equal(fs.existsSync(args[sessionIndex + 1]!), true);
+		assert.equal(path.dirname(args[sessionIndex + 1]!), openedSessionDirs[0]);
+		assert.equal(path.basename(openedSessionDirs[0]!), "run-0");
 	});
 
 	it("creates isolated forked sessions per parallel task", async () => {
-		const { manager, openedPaths, branchedLeafIds } = makeForkingSessionManagerRecorder({
+		const { manager, openedPaths, openedSessionDirs, branchedLeafIds } = makeForkingSessionManagerRecorder({
 			sessionFile: path.join(tempDir, "parent-parallel.jsonl"),
 			leafId: "leaf-777",
+			useSessionDir: true,
 		});
 		const executor = makeExecutor();
 
@@ -1204,6 +1211,8 @@ describe("fork context execution wiring", { skip: !available ? "subagent executo
 		const sessionArgs = readSessionArgsFromCalls();
 		assert.equal(sessionArgs.length, 2);
 		assert.equal(new Set(sessionArgs).size, 2);
+		assert.deepEqual(openedSessionDirs.map((dir) => path.basename(dir!)), ["run-0", "run-1"]);
+		assert.equal(new Set(openedSessionDirs).size, 2);
 		for (const childSessionFile of sessionArgs) {
 			assert.notEqual(childSessionFile, path.join(tempDir, "parent-parallel.jsonl"));
 			assert.equal(fs.existsSync(childSessionFile), true);
diff --git a/test/unit/fork-context.test.ts b/test/unit/fork-context.test.ts
index 69b3685..bb54c64 100644
--- a/test/unit/fork-context.test.ts
+++ b/test/unit/fork-context.test.ts
@@ -129,11 +129,12 @@ describe("createForkContextResolver", () => {
 		}
 	});
 
-	it("creates forked sessions through the default package opener", () => {
+	it("creates forked sessions through the default package opener in the configured child directory", () => {
 		const tempDir = fs.mkdtempSync(path.join(os.tmpdir(), "pi-subagents-fork-default-"));
 		try {
-			const sessionDir = path.join(tempDir, "sessions");
-			const parent = SessionManager.create(tempDir, sessionDir);
+			const parentSessionDir = path.join(tempDir, "sessions");
+			const childSessionDir = path.join(tempDir, "subagent-sessions", "run-0");
+			const parent = SessionManager.create(tempDir, parentSessionDir);
 			parent.appendMessage({ role: "user", content: "parent prompt" });
 			parent.appendMessage({ role: "assistant", content: "parent response" });
 			const parentSessionFile = parent.getSessionFile();
@@ -145,13 +146,17 @@ describe("createForkContextResolver", () => {
 			const resolver = createForkContextResolver({
 				getSessionFile: () => parentSessionFile,
 				getLeafId: () => leafId,
-				getSessionDir: () => sessionDir,
-			}, "fork");
+				getSessionDir: () => parentSessionDir,
+			}, "fork", {
+				sessionDirForIndex: () => childSessionDir,
+			});
 
 			const childSessionFile = resolver.sessionFileForIndex(0);
 			assert.ok(childSessionFile);
 			assert.notEqual(childSessionFile, parentSessionFile);
 			assert.equal(fs.existsSync(childSessionFile), true);
+			assert.equal(path.dirname(childSessionFile), childSessionDir);
+			assert.equal(childSessionFile.startsWith(`${parentSessionDir}${path.sep}`), false);
 		} finally {
 			fs.rmSync(tempDir, { recursive: true, force: true });
 		}
diff --git a/test/unit/notify.test.ts b/test/unit/notify.test.ts
index 916128b..75c2480 100644
--- a/test/unit/notify.test.ts
+++ b/test/unit/notify.test.ts
@@ -129,8 +129,9 @@ describe("registerSubagentNotify", () => {
 				customType: "subagent-notify",
 				content: "Background task completed: **worker**\n\n(no output)",
 				display: true,
+				details: { version: 1, runIds: ["notify-empty-1"] },
 			},
-			options: { triggerTurn: true },
+			options: { triggerTurn: true, deliverAs: "followUp" },
 		});
 	});
 
@@ -177,8 +178,9 @@ describe("registerSubagentNotify", () => {
 				customType: "subagent-notify",
 				content: "Detached foreground task completed: **reviewer**\n\nRecovered final review",
 				display: true,
+				details: { version: 1, runIds: ["foreground-run:0"] },
 			},
-			options: { triggerTurn: true },
+			options: { triggerTurn: true, deliverAs: "followUp" },
 		});
 	});
 
@@ -218,8 +220,9 @@ describe("registerSubagentNotify", () => {
 				customType: "subagent-notify",
 				content: `Background task completed: **worker** (2/3)\n\n${summary}`,
 				display: true,
+				details: { version: 1, runIds: ["notify-summary-1"] },
 			},
-			options: { triggerTurn: true },
+			options: { triggerTurn: true, deliverAs: "followUp" },
 		});
 	});
 
@@ -242,8 +245,9 @@ describe("registerSubagentNotify", () => {
 				customType: "subagent-notify",
 				content: "Background task completed: **worker**\n\nDone\n\nSession file: /tmp/session.jsonl",
 				display: true,
+				details: { version: 1, runIds: ["notify-path-1"] },
 			},
-			options: { triggerTurn: true },
+			options: { triggerTurn: true, deliverAs: "followUp" },
 		}]);
 	});
 
@@ -266,8 +270,9 @@ describe("registerSubagentNotify", () => {
 				customType: "subagent-notify",
 				content: "Background task paused: **worker**\n\nPaused after interrupt. Waiting for explicit next action.",
 				display: true,
+				details: { version: 1, runIds: ["notify-paused-1"] },
 			},
-			options: { triggerTurn: true },
+			options: { triggerTurn: true, deliverAs: "followUp" },
 		});
 	});
 
@@ -327,7 +332,7 @@ describe("registerSubagentNotify", () => {
 		assert.match(content, /^Background tasks completed \(3\): \*\*alpha\*\*, \*\*beta\*\*, \*\*gamma\*\*/);
 		assert.match(content, /1\. alpha\nalpha done/);
 		assert.match(content, /3\. gamma\ngamma done/);
-		assert.deepEqual(sent[0]!.options, { triggerTurn: true });
+		assert.deepEqual(sent[0]!.options, { triggerTurn: true, deliverAs: "followUp" });
 	});
 
 	it("ignores successes from other sessions instead of grouping them", () => {
