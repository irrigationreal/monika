{
  "name": "@irrigationreal/monika-agentd",
  "version": "0.1.0",
  "license": "AGPL-3.0-or-later",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.21.0",
  "dependencies": {
    "@earendil-works/pi-ai": "0.85.1",
    "@earendil-works/pi-coding-agent": "0.85.1"
  },
  "scripts": {
    "start": "node src/server.mjs",
    "test": "sh -c 'root=$(mktemp -d); trap \"rm -rf $root\" EXIT; PI_SUBAGENT_RUNTIME_ROOT=$root/runtime PI_SUBAGENT_SESSION_ROOT=$root/sessions PI_SUBAGENT_OPERATOR_ROOT=$root/operator MONIKA_RUNTIME_INSTANCE_FILE=$root/runtime-instance.json node --test test/*.test.mjs'"
  }
}
